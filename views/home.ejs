<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>URL Shortener | Smart Link Management</title>

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Inter', sans-serif;
    }

    body {
      min-height: 100vh;
      background: radial-gradient(circle at top, #4f46e5, #020617);
      display: flex;
      align-items: center;
      justify-content: center;
      color: #0f172a;
    }

    .card {
      width: 100%;
      max-width: 520px;
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      border-radius: 24px;
      padding: 40px 32px;
      box-shadow: 0 25px 60px rgba(0,0,0,0.25);
      text-align: center;
      animation: fadeIn 0.6s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .logo {
      font-size: 28px;
      font-weight: 700;
      margin-bottom: 6px;
      color: #1e293b;
    }

    .subtitle {
      font-size: 14px;
      color: #64748b;
      margin-bottom: 28px;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 14px;
    }

    .input-group {
      display: flex;
      border-radius: 12px;
      overflow: hidden;
      border: 1px solid #e2e8f0;
      transition: 0.2s ease;
    }

    .input-group:focus-within {
      border-color: #4f46e5;
      box-shadow: 0 0 0 3px rgba(79,70,229,0.15);
    }

    input[type="url"] {
      flex: 1;
      padding: 14px;
      border: none;
      outline: none;
      font-size: 14px;
    }

    button {
      background: #4f46e5;
      color: #fff;
      border: none;
      padding: 0 18px;
      font-weight: 600;
      cursor: pointer;
      transition: 0.2s ease;
    }

    button:hover {
      background: #4338ca;
    }

    .result {
      margin-top: 24px;
      background: #f1f5f9;
      border-radius: 14px;
      padding: 18px;
      font-size: 14px;
      text-align: left;
    }

    .result-title {
      font-weight: 600;
      margin-bottom: 8px;
      color: #0f172a;
    }

    .short-link {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      background: #fff;
      border-radius: 10px;
      padding: 10px 12px;
      border: 1px solid #e2e8f0;
    }

    .short-link a {
      color: #4f46e5;
      text-decoration: none;
      font-weight: 500;
      word-break: break-all;
    }

    .copy-btn {
      background: #0f172a;
      padding: 6px 12px;
      border-radius: 8px;
      font-size: 12px;
    }

    .copy-btn:hover {
      background: #020617;
    }

    .footer {
      margin-top: 28px;
      font-size: 12px;
      color: #94a3b8;
    }

    .footer strong {
      color: #0f172a;
    }
  </style>
</head>
<body>
  <div class="card">
    <div class="logo">🔗 LinkShort</div>
    <div class="subtitle">Fast, secure & production‑ready URL shortening</div>

    <form action="/shorten" method="POST">
      <div class="input-group">
        <input
          type="url"
          name="fullUrl"
          placeholder="Paste your URL here..."
          required
        />
        <button type="submit">Shorten</button>
      </div>
    </form>

    <% if (shortUrl) { %>
      <div class="result">
        <div class="result-title">Your shortened link</div>

        <div class="short-link">
          <a id="shortUrl" href="/<%= shortUrl %>" target="_blank">
            <%= baseUrl %>/<%= shortUrl %>
          </a>
          <button class="copy-btn" onclick="copyLink()">Copy</button>
        </div>
      </div>
    <% } %>

    <div class="footer">
      Built with <strong>Node.js</strong>, <strong>Express</strong> & <strong>MongoDB</strong>
    </div>
  </div>

  <script>
    function copyLink() {
      const link = document.getElementById('shortUrl').innerText;
      navigator.clipboard.writeText(link).then(() => {
        alert('Copied to clipboard!');
      });
    }
  </script>
</body>
</html>
